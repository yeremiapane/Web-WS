version: '3.8'

services:
  allure-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: allure-landing-app
    ports:
      - "4000:5173"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - VIRTUAL_HOST=${DOMAIN:-localhost}
      - VIRTUAL_PORT=5173
    networks:
      - allure-network

networks:
  allure-network:
    driver: bridge
